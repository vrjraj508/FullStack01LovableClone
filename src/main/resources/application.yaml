spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/lovable
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true


server:
  port: 8081